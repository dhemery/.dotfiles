[color]
	branch = auto
	diff = auto
	grep = auto
	status = auto

[commit]
	verbose = true

[core]
	autocrlf = input
	pager = less -FRX

[diff]
	tool = opendiff

[push]
	default = current

[rerere]
	enabled = true
