[color]
	branch = auto
	diff = auto
	grep = auto
	status = auto

[core]
	autocrlf = input
	pager = less -FRX

[diff]
	tool = opendiff

[include]
	path = ~/.dale/git/config

[push]
	default = current

[rerere]
	enabled = true
